{
  "name": "dungeon-cards",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "cards": "bun --watch run src/server.ts",
    "gen": "bun run src/gen.ts",
    "check": "bun run src/check.ts",
    "clean-art": "bun run src/cleanup.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
